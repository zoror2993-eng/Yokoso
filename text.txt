<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Happy Birthday Sahil bhaiya</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      background: linear-gradient(to right, #ff9a9e, #fad0c4);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      color: #333;
    }
    h1 {
      font-size: 2.5em;
      margin-bottom: 20px;
    }
    #countdown {
      font-size: 2em;
      font-weight: bold;
      background: #fff;
      padding: 15px 30px;
      border-radius: 10px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.3);
    }
    .birthday-message {
      display: none;
      font-size: 2.5em;
      font-weight: bold;
      color: #ff4081;
      animation: pop 1s infinite alternate;
    }
    @keyframes pop {
      from { transform: scale(1); }
      to { transform: scale(1.1); }
    }
  </style>
</head>
<body>
  <h1>Countdown to Sahil bhaiya's Birthday 🎂</h1>
  <div id="countdown"></div>
  <div class="birthday-message">🎉 Happy Birthday Sahil bhaiya🫂 🎉</div>

  <script>
    // Set the target date - 19 June of current year
    const today = new Date();
    let year = today.getFullYear();
    const targetDate = new Date(year, 5, 19, 0, 0, 0); // Months are 0-indexed (5 = June)

    // If today is already past 19 June, set it for next year
    if (today > targetDate) {
      targetDate.setFullYear(year + 1);
    }

    const countdownEl = document.getElementById("countdown");
    const birthdayMessage = document.querySelector(".birthday-message");

    function updateCountdown() {
      const now = new Date().getTime();
      const distance = targetDate.getTime() - now;

      if (distance <= 0) {
        countdownEl.style.display = "none";
        birthdayMessage.style.display = "block";
        clearInterval(interval);
      } else {
        const days = Math.floor(distance / (1000 * 60 * 60 * 24));
        const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
        const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((distance % (1000 * 60)) / 1000);

        countdownEl.innerHTML = `${days}d ${hours}h ${minutes}m ${seconds}s`;
      }
    }

    // Update countdown every second
    const interval = setInterval(updateCountdown, 1000);
    updateCountdown();
  </script>
</body>
</html>
